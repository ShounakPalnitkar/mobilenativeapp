{
  "name": "ckd-prediction-tool",
  "version": "1.0.0",
  "description": "Chronic Kidney Disease Prediction Tool (Web Version)",
  "main": "index.js",
  "scripts": {
    "start": "serve -s www",
    "build": "npm install --legacy-peer-deps && mkdir -p www && cp src/index.html www/ && cp -r src/assets www/",
    "render-build": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf node_modules package-lock.json"
  },
  "dependencies": {
    "@capacitor/core": "5.0.0",
    "serve": "14.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "5.0.0",
    "eslint": "8.56.0"
  }
}
